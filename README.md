# spothistory
Script automatizado que usa GitHub Actions para obtener cada 15 minutos mis reproducciones recientes de Spotify y guardarlas en un CSV. Mantiene un historial completo y sin duplicados sin necesidad de tener el ordenador encendido.

ACTUALIZACIÓN: ahora hace lo mismo, pero persiste los datos directamente en una base de datos (usando Supabase).
